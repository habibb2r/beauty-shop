import React from 'react';
import SectionTitle from '../../../../Components/Reuseable/SectionTitle';

const AllItems = () => {
    return (
        <div>
            <SectionTitle title={"All Items"}></SectionTitle>
        </div>
    );
};

export default AllItems;